================================================================================
 Bu D3eij — NEW PC MIGRATION PROMPT
 Paste everything below this header into an AI coding agent (e.g. Claude Code)
 running on the NEW machine. It will set the app up end to end.
================================================================================

BEFORE YOU PASTE — copy these from the OLD PC first (the one thing that cannot
be re-downloaded is marked):

  1. %USERPROFILE%\.bud3eij\models\vanguard\   <-- CRITICAL: model.onnx (1.7 GB)
     + tokenizer.json. This model is NOT hosted anywhere — if it is lost it must
     be re-exported from scratch (see _export_vanguard.py notes in CLAUDE.md).
  2. %USERPROFILE%\.bud3eij\models\            (optional: all other models —
     saves ~1.5 GB of re-downloads, everything in it EXCEPT vanguard/ can be
     re-downloaded automatically on first use)
  3. %LOCALAPPDATA%\Bu D3eij\history.json      (optional: the Recent history)

--------------------------------- PROMPT ---------------------------------

You are setting up "Bu D3eij" — my personal Windows desktop app (file converter
+ AI image/text/audio tools, CustomTkinter GUI) — on this new PC. The private
repo is https://github.com/Kha73k/Bu-D3eij (ask me to authenticate GitHub if
needed). Work through ALL the steps below, verify each one, and fix problems
yourself. Read CLAUDE.md in the repo before doing anything — it is the source
of truth and overrides this prompt where they disagree.

1. PREREQUISITES (install what's missing):
   - Python 3.11 (winget install Python.Python.3.11)
   - Git, and ffmpeg via: winget install Gyan.FFmpeg
     (ffmpeg lands on PATH for NEW terminals only — open a fresh shell or
     prepend its bin to $env:PATH in yours)
   - Check for an NVIDIA GPU (Get-CimInstance Win32_VideoController). The AI
     tools (Demucs stems, UltraSharp upscaler, BiRefNet bg removal) use CUDA
     when present and fall back to CPU (much slower) when not.
   - Microsoft Word / PowerPoint are optional (high-fidelity DOCX/PPTX -> PDF;
     the app falls back to text-only PDFs without them).

2. CLONE + ENVIRONMENT:
   - git clone https://github.com/Kha73k/Bu-D3eij "Bu D3eij" ; cd into it.
   - py -3.11 -m venv .venv
   - IMPORTANT ORDER (torch must come from the PyTorch index, NOT plain PyPI):
       .\.venv\Scripts\python -m pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu126
       (no NVIDIA GPU? use --index-url https://download.pytorch.org/whl/cpu)
       .\.venv\Scripts\python -m pip install -r requirements.txt
   - KNOWN TRAP: if anything later reinstalls torchvision from plain PyPI, it
     becomes the CPU build and mismatches CUDA torch — fix with:
       pip install torchvision --index-url https://download.pytorch.org/whl/cu126 --force-reinstall --no-deps
   - Sanity check: .\.venv\Scripts\python -c "import torch; print(torch.cuda.is_available())"

3. MODEL FILES:
   - I may have copied %USERPROFILE%\.bud3eij\models\ from my old PC — if so,
     verify vanguard\model.onnx (~1.7 GB) and vanguard\tokenizer.json exist.
     If the vanguard folder is MISSING, warn me loudly: the AI Text Detector
     will not work until I restore it from a backup (it cannot be downloaded).
   - Everything else self-downloads on first use into %USERPROFILE%\.bud3eij\
     models\ and %USERPROFILE%\.u2net\ — no action needed.
   - Optionally restore history.json to %LOCALAPPDATA%\Bu D3eij\.

4. VERIFY FROM SOURCE (run with the venv python, fresh shell so ffmpeg is on
   PATH):
   - tests\test_gui_smoke.py   (fast; all pages + widgets)
   - tests\test_headless.py    (slow: loads/downloads every model — confirms
     conversions, upscaler, bg removal, OCR, font ID, detector, stem splitter)
   - tests\test_com_paths.py   (only if Word/PowerPoint are installed)
   - A quick real check of each direction:
       python app.py --convert <some.png> jpg
       python app.py --upscale <some.png> 1080p
       python app.py --split-stems <some.mp3>   (first run downloads ~320 MB)

5. BUILD THE STANDALONE EXE:
   - pip install pyinstaller, then run the exact build command from CLAUDE.md's
     "Commands" section (do not improvise flags — every --collect-all and
     --exclude-module exists for a documented reason). Expect a ~4.6 GB
     dist\Bu D3eij\ folder and a slow build (bundled CUDA torch).
   - Verify the exe headless: --convert, --extract-text, --remove-bg Omega,
     --split-stems, and a GUI boot (start it, confirm it stays alive ~10 s).
   - Make me a desktop shortcut to "dist\Bu D3eij\Bu D3eij.exe".

6. WRAP UP:
   - Run the GUI once (python app.py) and confirm every sidebar page opens.
   - Tell me anything that failed or was skipped, and remind me to set up
     GitHub push access (git remote) so future work can be committed.

------------------------------- END PROMPT -------------------------------
