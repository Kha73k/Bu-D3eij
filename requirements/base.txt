# Core — always installed. File Converter + Nexus utilities + YouTube + ASCII Art.
# No ML runtime, no torch, no onnxruntime. (numpy/Pillow are small and shared by
# the optional groups, so they live here.)
customtkinter>=5.2.2
tkinterdnd2>=0.4.2
Pillow>=11.0.0
numpy>=1.26
truststore>=0.10        # route SSL through the OS trust store (standalone Python
                        # ships no CA bundle -> urllib downloads else fail)
# Document / AV converters
python-docx>=1.1.2
pdfplumber>=0.11.4
reportlab>=4.2.0
ffmpeg-python>=0.2.0
pdf2docx>=0.5.8          # pulls PyMuPDF (AGPL-3.0) — see THIRD_PARTY.md
python-pptx>=1.0.2
pymupdf4llm>=0.0.17      # pulls PyMuPDF (AGPL-3.0) — see THIRD_PARTY.md
mammoth>=1.8.0
markdownify>=0.13.1
# YouTube
yt-dlp>=2025.1.1
# Nexus utilities
pint>=0.23
tzdata>=2024.1
qrcode[pil]>=7.4
# Windows COM (Word/PowerPoint high-fidelity PDF) + clipboard
pywin32>=306 ; platform_system == "Windows"
