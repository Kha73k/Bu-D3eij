# ---------------------------------------------------------------------------
# Bu D3eij — DEV "everything" install (all features).
#
# The feature-selective installer composes per-feature subsets from requirements/
# instead of using this file — see requirements/README.md and docs/DISTRIBUTION.md.
#
# PyTorch is installed from the PyTorch index (NOT plain PyPI). Install ONE torch
# group FIRST, so demucs/spandrel resolve against it:
#   pip install -r requirements/torch-cpu.txt      # default
#   pip install -r requirements/torch-cuda.txt     # NVIDIA GPU (cu126)
# Then this file:
#   pip install -r requirements.txt
# ---------------------------------------------------------------------------
-r requirements/base.txt
-r requirements/marquee.txt
-r requirements/vanguard.txt
-r requirements/sonara.txt
